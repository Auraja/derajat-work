"use client"; import { useParams } from "next/navigation"; import { GeneratorForm } from "@/components/generator-form"; export default function Page(){const {slug}=useParams<{slug:string}>();return <GeneratorForm kind="slides" slug={slug}/>}
